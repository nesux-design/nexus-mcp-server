OAuth authorization initiation endpoints (`/oauth/<provider>`) must remain publicly reachable so a user can begin the provider authorization flow. The OAuth callback remains public because it is called by the provider, while token storage and MCP/provider API routes must remain protected by the gateway authentication layer. Do not require `NEXUS_INTERNAL_AUTH_SECRET` on the OAuth initiation or callback routes.